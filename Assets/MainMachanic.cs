using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering.UI;

public class MainMachanic : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // tutorial pop up
        // UI text to intruduse the game
        // 
    }

    // Update is called once per frame
    void Update()
    {
        //walking sound script
        // backgrouond sound.
        //

        //check if player is looking at creature then move the creature

        // maybe have the creature guarding an object that needs to be lured away before you can proceed
    }
}
